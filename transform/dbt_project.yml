name: 'upstrace'
version: '1.0.0'
profile: 'upstrace'

model-paths: ["models"]
target-path: "target"
clean-targets: ["target", "dbt_packages"]

models:
  upstrace:
    staging:
      +materialized: view